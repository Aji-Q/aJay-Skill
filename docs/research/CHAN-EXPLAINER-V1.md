# J Trader · 图上讲解 / 建筑研究导航

> 历史版本 v1：其中的虚拟专家肖像、侧栏布局和旧建筑尺寸已由 [工作台 v2](REPORT-CHART-V2.md) 替换。以下保留当时实现和验收记录，不作为当前页面能力说明。

## 本轮范围

- 本地交付，不提交或推送 GitHub。
- 默认报告仍是单文件、离线可读的连续研究网站，保留估值、来源、搜索、打印与导出。
- 原有八位人物为方法论模拟；新增原创缠论分析师为 AI 方法讲解，不代表真实投资者参与。
- 缠论步骤解释当前输入快照，不是逐日回测，也不是自动交易。

## 功能结构

1. 同源日线输入 → CZSC 1.0.1 结构 → `2_kline.data.chan` 的版本化结果。
2. 日线最近 2 年；周线由至多 6 年的同源日线按交易周聚合。
3. SVG、候选点表、专家解释使用相同结构 ID、日期和价格端点。
4. 支持分型、完成笔、未完成笔、笔级中枢，以及明确标为候选的一买/一卖力度代理与三买/三卖。
5. 无二买/二卖、线段、严格 MACD 背驰、信号库数量噱头或在线问答入口。

## 视觉结构

- 上海照片采用已保存实拍原片的完整 4994 × 11270 像素，转换为 WebP；页面以 CSS 做轻微调色，不再放大旧的 835 × 1884 AI 衍生素材。
- 图片宽度决定自然高度；完整财务/护城河证据位于建筑叙事之后，打开长表格不会改变图片尺寸。
- 回报记录、增长来源、治理根基分别关联楼身三个标记。标记位置是研究导航，不表示财务分值或建筑真实楼层用途。
- 图表静态可读；只对当前讲解的结构定向绘制。不对蜡烛或报价做数字跳动。
- 深浅主题通过语义变量映射旧片段的警示、目标同行与正负背景，不篡改原始记录或 SVG 涨跌颜色。

## 可执行验收清单

- [x] 实际 AAPL 日/周结构、图表/点位/解释坐标对应
- [x] 固定样本：无中枢、未完成笔、缺失数据、非有限数值、单周期失败
- [x] 日/周、全部/隐藏、重新讲解、上/下一步、记录定位
- [x] 上海自然尺寸、三个锚点、长证据展开尺寸不变
- [ ] 人像 RGBA、面部保留、边缘无矩形底/白边
- [x] 390 × 844、768 × 1024、1280 × 720、1440 × 900
- [x] 深浅主题、减少动态、离线/无脚本、打印
- [x] 原有搜索、来源弹窗、导出入口
- [x] 完整回归测试与独立美学审阅

验收结果与数据截止时间以本轮完成记录为准；未通过项目保持显式未完成状态。

## 本轮实测记录（2026-09-10）

### 数据与可解释性

- 正式 `fetch_kline.main('AAPL')` 已实际运行；命中 `yfinance.Ticker.history`，复权状态 `applied`，同一份 1506 根日线输入生成两级结构。
- 数据截止 2026-09-10：D 502 根 K 线、42 根完成笔、6 个至少三笔的中枢；W 314 根 K 线、19 根完成笔、4 个至少三笔的中枢。
- 当前 D 含一个一卖力度代理候选：2026-07-29、344.27309743；突破该观察极值为候选失效条件，不是交易指令。W 当前没有候选。此处数据仅用于记录本次测试快照，不是持续更新报价。
- 修正了 CZSC 尾部只有两笔也出现在其 `zs_list` 的情况：本产品中枢至少要求三个可映射完成笔且 `zg > zd`，不足者不用于候选计算。
- 2026-09-10 为周四，最后一根周线明确标记 `in_progress`，不当作已确认周收盘信号。
- 仅刷新 `2_kline`；其它财务、讨论、综合结论保持原 2026-09-09 快照，页面有“局部数据更新”提示。原缓存备份为 `.cache/AAPL/raw_data.before-chan-v1.json`。原始数据缺口及未复核状态没有被清除。

### 本地交付边界

- 人物尺寸与伴随布局已接入。真实透明背景素材仍待完成：两次内置生图返回无 alpha 的 RGB，已拒绝安装；使用原有写真作临时显示，没有用棋盘格冒充透明。
- 原创 AI 缠论分析师写真已生成，并标为非真实人物、AI 方法讲解。现有肖像身份未替换。
- 所有人物抠图完成前，“透明轮廓”项仍为未通过；相关尝试记录见 `skills/deep-analysis/assets/ajay-speakers/manifest.json`。
- 本轮不提交、不推送 GitHub，不增加外部问答、语音、自动交易或远程上传。

### 已完成交互核验

- 上海 `<img>` 实际加载 `4994 × 11270`；1440 宽下显示为约 `691.2 × 1559.8`，没有强制拉伸。
- 展开完整财务原始记录后，楼身高度仍为 `1559.8px`。
- 新摘要按钮打开正确财务来源；关闭弹窗后焦点返回原按钮。
- 讨论检索“巴菲特”显示对应记录，清空后恢复；导出面板与打印入口保留，不自动上传。
- 独立 GPT-6 Astra Low 美学审阅认可上海视觉方向、确认从照片展示转为功能驱动；主图密度和头部高度作为收口改进。


### 收口验收

- 六步实际点击结果：全览 215 组日线结构；第 1 步 0 组结构、第 2 步 207 组分型/完成笔；第 5 步显示候选但隐藏失效线，第 6 步绘制来源 ID `D:signal:0:invalid:above` 的实际条件线。隐藏=0；重新讲解回到第 1 步。
- 选中唯一候选，图中锚点、点位行、侧边说明均显示 2026-07-29 / 344.27 / 一卖力度代理及突破 344.27 的失效条件；说明文本从同一行来源字段读取，不额外生成价格。
- 独立逐坐标审计：61 根完成笔全部匹配；10 个中枢共 20 条上下边界全部匹配；SVG 四舍五入误差小于 0.05 坐标单位。所有时间锚点不超过本次快照截止日期；这不等于历史回测无前视偏差的证明。
- 390 / 768 / 1280 / 1440 宽均无页面横向溢出；手机与平板图表→人物解释→档案/点位表，720px 内部横向画幅保持文字清晰，默认定位近期价格，可左右滑动及按记录定位。
- 手动停用动态后活动画线属性归零；系统 `prefers-reduced-motion` 开启时即使启用按钮也保持无动态。
- CDP 断网后日/周切换与全部标注继续工作；禁用脚本重新加载，D/W 502/314 根 K 线及全部 12 步静态说明都可见。
- 从“隐藏标注”进入打印状态，两级图、全部结构与条件线恢复，采用浅色打印文字与背景；原始窗口恢复后状态仍可继续操作。
- 深链接在内嵌字体完成加载后自动定位，读者已有点击/滚动时不抢回滚动位置。
- 最终截图保存在同目录 `ui-qa-chan-v1`，以 `*-final.png` 为最新验收图；早期截图不是最终效果。
- 已通过 1063 项回归；环境仍有 5 个既有 pandas/protobuf 依赖警告，未为本轮 UI 升级擅自变更这些环境包。
- 唯一未通过项：人物真实透明轮廓素材，等待本地抠图方式确认。当前实际显示仍是原写真。

### 交付入口

- 本地预览：`http://127.0.0.1:8787/AAPL_20260910/full-report-standalone.html?v=chan-desk-v3#chan-workspace`
- 修改源：`skills/deep-analysis/assets/report-continuous.{html,css,js}`、`report-chan.{css,js}`、`report-chan-table.css`，以及 `skills/deep-analysis/scripts/lib/report/{continuous_renderer,chan_desk,chan_table}.py`。
- 报告是生成产物，请从以上源文件修改再重新组装；直接编辑报告会被下次生成覆盖。
